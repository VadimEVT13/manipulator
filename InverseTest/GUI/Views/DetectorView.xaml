<UserControl x:Class="InverseTest.GUI.Views.DetectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:fa="http://schemas.fontawesome.io/icons/"
			 xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:local="clr-namespace:InverseTest.GUI.Views"
             mc:Ignorable="d" 
             >

    
	<UserControl.InputBindings>
		<KeyBinding Command="{Binding Open}" Key="O" />
		<KeyBinding Command="{Binding Close}" Key="C" />

		<KeyBinding Command="{Binding PX}" Key="Q" />
		<KeyBinding Command="{Binding MX}" Key="A" />

		<KeyBinding Command="{Binding PY}" Key="W" />
		<KeyBinding Command="{Binding MY}" Key="S" />

		<KeyBinding Command="{Binding PZ}" Key="E" />
		<KeyBinding Command="{Binding MZ}" Key="D" />

		<KeyBinding Command="{Binding PA}" Key="R" />
		<KeyBinding Command="{Binding MA}" Key="F" />

		<KeyBinding Command="{Binding PB}" Key="T" />
		<KeyBinding Command="{Binding MB}" Key="G" />
	</UserControl.InputBindings>

	<StackPanel>
		<TextBlock Margin="5" TextAlignment="Left" Text="{Binding Status}"/>
		<StackPanel Orientation="Horizontal">
			<Button Command="{Binding PlugCommand}">
				<Image Source="{Binding PlugImage}" Width="20" Height="20"/>
			</Button>
			<Button Command="{Binding PlayCommand}">
				<Image Source="{Binding PlayImage}" Width="20" Height="20"/>
			</Button>

			<!--StackPanel>
                <Button  Content="Старт" Command="{Binding Start}" />
                <Button  Content="Пауза" Command="{Binding Pause}" />
			</StackPanel-->
			<Button Command="{Binding UnlockCommand}">
				<Image Source="{Binding UnlockImage}" Width="20" Height="20"/>
			</Button>
			<Button Command="{Binding HomeCommand}">
				<Image Source="{Binding HomeImage}" Width="20" Height="20"/>
			</Button>
			
			<!--StackPanel>
				<Button  Content="Домой" Command="{Binding Home}" />
				<Button  Content="Разблокировать" Command="{Binding Unlock}" />
			</StackPanel-->
		</StackPanel>

        </StackPanel>
</UserControl>